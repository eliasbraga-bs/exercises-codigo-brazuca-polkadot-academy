// 2. Crie uma função que some dois números.
function somarNumero(numero1, numero2) {
  return numero1 + numero2;
}

let resultado = somarNumero(9, 19);

console.log(`A soma dos números é: ${resultado}`);
